# Bagimliliklarin TEK kaynagi. Hem Makefile ($(DEPS)) hem Dockerfile burayi
# okur — iki ayri liste tutulursa biri sessizce eskir.
#
# Surumler sabitlenmedi: gelistirme akisi bugun boyle. Yayinda tekrarlanabilir
# imaj isteniyorsa burayi `uv pip compile` ciktisiyla degistir.

fastapi
uvicorn[standard]
jinja2
python-multipart
httpx
authlib
itsdangerous
python-dotenv
psycopg[binary,pool]         # PostgreSQL (spec/80-veritabani.md)
pywebpush                    # web push (spec/40-push.md)
Pillow                       # medya isleme: yeniden kodlama, EXIF, kucuk resim (spec/ ekler)
